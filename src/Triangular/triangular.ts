
// const triangular = ( number ) => {
//   return ( number > 0 )
//     ? number * ( number + 1 ) / 2
//     : 0;
// };


const triangular = ( result: number ) => {
  return ( result > 0 )
    ? result * ( result + 1 ) / 2
    : 0;
};

export default triangular;